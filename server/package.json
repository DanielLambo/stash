{
  "name": "clipboard-sync-server",
  "version": "1.0.0",
  "description": "Local sync server for the Clipboard Chrome extension.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
